import styles from './DividerVertcalIcon.module.css';
import { RxDividerVertical } from 'react-icons/rx';

export default function DividerVertcalIcon() {
    return (
        <RxDividerVertical className={styles.divider} />
    );
}
